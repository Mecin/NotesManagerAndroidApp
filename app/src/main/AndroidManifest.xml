<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.dmcs.mecin.notesmanager" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".NotesManager"
            android:label="@string/app_name" >
            <intent-filter>
                <provider android:name="pl.dmcs.notesmanager.providers.NotesManagerProvider" android:authorities="pl.dmcs.notesmanager.providers.NotesManagerProvider"/>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
